# paas-cli

# setup

requires:

- rbenv
- aws-vault
- make

```
gem install bundler
bundle install
```

add the following to your shell profile, _AFTER_ rbenv init

```
export PATH="/path/to/repo/bin:$PATH"
```
